/**
 * @param {number[][]} mat
 * @return {number[][]}
 */
var updateMatrix = function(mat) {
    
};

function Solve(mat , ans , i , j ){

}


let mat = [[0,0,0],[0,1,0],[0,0,0]];


// `

`
traverse through the matrix until no 1 found during traversal;

once 1 found in the matrix make the function call in 4 possible directions and find the zero distances

and find the minimum from 4 of them and place in the new matrix 

`